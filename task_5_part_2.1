import json

class Model:

	def __init__(self):
		self.title = input('')
		self.text = input('')
		self.author = input('')


	def save(self):
		with open('use.json', 'w') as f:
			d = self.__dict__
			json.dump(d, f)
		
s = Model()
s.save()
